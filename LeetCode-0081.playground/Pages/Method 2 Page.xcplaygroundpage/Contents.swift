//: [Previous](@previous)

/*:
 # 方法二：一行
 
 直接调 `Swift` 数组的 API
 */

class Solution {
    func search(_ nums: [Int], _ target: Int) -> Bool {
        return nums.contains(target)
    }
}

//: [Next](@next)
